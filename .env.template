# Factory Intelligence System - Environment Variables

# AWS Bedrock Configuration (replaces direct Anthropic API)
# Ensure AWS credentials are configured via ~/.aws/credentials or IAM role
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-20250514-v1:0

# Server configuration
PORT=3000

# MQTT Broker (ProveIt! Virtual Factory)
MQTT_HOST=mqtt://virtualfactory.proveit.services:1883
MQTT_USERNAME=proveitreadonly
MQTT_PASSWORD=proveitreadonlypassword

# InfluxDB Configuration
INFLUXDB_URL=http://localhost:8086
INFLUXDB_TOKEN=proveit-factory-token-2026
INFLUXDB_ORG=proveit
INFLUXDB_BUCKET=factory
